import React from "react";

function Secret(){
    // if (!solved) return <Redirect to="/" />

    return (
        <div>
            <h1>You Solved It!</h1>
        </div>
    )
};

export default Secret;